// Every stat here must map to something a visitor can independently verify:
// a dated role, a live URL, or a public repo. No self-referential counts.

export const PORTFOLIO_STATS = {
  experienceYears: 1.5,
  liveProducts: 6,
  aiProducts: 3,
  openSourceProjects: 4,
} as const;
